apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pluralsite-deployment
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ceeb0t/kub.git
    path: Deployments
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      enabled: true
